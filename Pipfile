[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
invenio = {version = ">=3.0.0",extras = ["base", "metadata", "postgresql", "auth", "elasticsearch5", ]}
inspirehep = {editable = true,path = "."}
uwsgi = ">=2.0"
inspire-schemas = ">=59.0.0"
requests-oauthlib = "==1.0.0"
# pre-release is enable globally
marshmallow = "<3.0.0"

[dev-packages]
check-manifest = ">=0.35"
coverage = ">=4.4.1"
isort = ">=4.3"
mock = ">=2.0.0"
pydocstyle = ">=2.0.0"
pytest = ">=3.3.1"
pytest-cov = ">=2.5.1"
pytest-invenio = ">=1.0.2,<1.1.0"
pytest-mock = ">=1.6.0"
pytest-pep8 = ">=1.0.6"
pytest-random-order = ">=0.5.4"
pytest-runner = ">=3.0.0,<5"
# black = "==18.9b0"

[requires]
python_version = "3.6"

[scripts]
test = "python setup.py test"

[pipenv]
allow_prereleases = false
