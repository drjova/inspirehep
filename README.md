# Inspirehep

### Installation

#### pre-commit
Install ``pre-commit`` https://pre-commit.com

#### pyenv (optional)
install ``pyenv`` https://github.com/pyenv/pyenv

```bash
$ pip install pipenv
```

### How to run

```bash
$ docker-compose up -d
$ ./script/bootstrap
$ ./script/setup
$ ./script/server
```